# workshop.yaml
name: opencode
base: ubuntu@24.04
sdks:
  - name: opencode
    channel: latest/stable
  - name: direnvrc
    channel: latest/stable
  - name: uv
    channel: latest/stable

actions:
  opencode: direnv exec /project opencode "$@"
